import React, { Component } from "react";
import { navigate } from "@reach/router";
import "../../utilities.css";

class StartGame extends Component {
    constructor(props){
        super(props);
    }

    render(){
        return (<div>This is the start game page</div>);
    }
}

export default StartGame;